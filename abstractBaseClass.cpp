#include<iostream>
using namespace std;

// Abstract Base class: a class where at least one pure virtual function is used
// such class that is just made to inherit rather than its own use is called abstract base class
// virtual void display()=0 is used 
//=0 means it's compulsory to override this func
int main(){
    
    return 0;
}